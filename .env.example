# Optional provider configuration placeholders. Never put real secrets here.
LLM_PROVIDER=mock
IMAGE_PROVIDER=mock
VIDEO_PROVIDER=mock
VOICE_PROVIDER=mock
TRANSCRIPTION_PROVIDER=mock
STORAGE_PROVIDER=local
